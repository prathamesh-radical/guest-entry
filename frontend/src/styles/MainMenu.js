import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'center',
    },
    menu: {
        marginTop: 40,
        backgroundColor: "#fff",
        borderRadius: 10,
        backgroundColor: '#fff',
    },
    contentStyle: {
        backgroundColor: '#fff',
        borderRadius: 10,
    },
    menuItem: {
        color: "#000",
    },
    titleStyle: {
        color: "black",
    },
    divider: {
        height: 1,
        backgroundColor: '#ccc',
    },
});